import React from 'react';
import PropTypes from 'prop-types';

const MainPresenter = ({ whatToday }) => {
  return <div>{whatToday}</div>;
};

MainPresenter.propTypes = {};

export default MainPresenter;
